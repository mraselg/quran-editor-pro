╔══════════════════════════════════════════════════════════════════════════════╗
║          Studio Al-Qalam — Task Continuation Handoff Prompt                ║
║          GitHub: https://github.com/mraselg/quran-editor-pro               ║
╚══════════════════════════════════════════════════════════════════════════════╝

GITHUB REPOSITORY:
  https://github.com/mraselg/quran-editor-pro

LATEST COMMIT (as of 2026-05-22):
  4e91471 — feat: performance optimizations (Canvas measurement, Virtualized
            PageList, Diff history, DAL, Optimistic Reflow)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
CONTEXT: WHAT THIS PROJECT IS
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

"Studio Al-Qalam" is a high-precision Quranic DTP (Desktop Publishing) editor
built with:
  - TanStack Start (React meta-framework with file-based routing)
  - Zustand (state management) + zundo (temporal/undo middleware)
  - Tailwind CSS v4
  - react-window v2 (virtualized PageList — 1740 pages)

The app renders 1740 pages of the full Quran, allowing per-row, per-page,
per-surah, and global overrides of Arabic/Bangla font sizes, positions,
and tajweed symbols.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
VERIFIED WORKING (as of 2026-05-22)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

All of the following have been browser-tested and confirmed working:

[✓] App loads — 1740 Quran pages render with correct typography and borders
[✓] Sidebar PageList virtualized scrolling — smooth, no DOM lag
[✓] Arabic font size slider → instant reflow (no freeze)
[✓] Bangla font size slider → instant reflow
[✓] Ctrl+Z / Ctrl+Shift+Z Undo/Redo — works correctly
[✓] History dropdown — shows Bengali labels e.g. "আরবি ফন্ট সাইজ: 50 → 66"
[✓] "আগের" (Preview Before) button — shows old value for 5s then restores
[✓] Tajweed symbols — properly positioned above Arabic characters
[✓] No console errors (only hydration warning from browser extension attrs)
[✓] Build: vite build exits code 0 — both client + SSR

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
HOW TO START (for a new agent or new chat session)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

1. Install dependencies (if node_modules missing):
     npm install

2. Start dev server:
     npm run dev -- --port 5174

3. App URL: http://localhost:5174/

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
FUTURE WORK (Phase 3 — NOT yet started)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

[ ] SQLite Migration (Electron)
    - Implement ElectronDAL in src/data/dal.ts using better-sqlite3 or Prisma
    - Move verses.json (5.6 MB) to SQLite `verses` table
    - Move pages.json to SQLite `pages` table
    - Support LIMIT/OFFSET queries for lazy page loading

[ ] Custom Tajweed Icon Font (.woff2)
    - Replace inline SVG tajweed symbols with a custom woff2 font
    - Use Glyphr Studio or IcoMoon to create tajweed-symbols.woff2
    - Update src/tajweed/svgMap.ts → fontCharMap.ts

[ ] Chunked Page Building (if buildAllPages() is still slow)
    - If 600+ pages are slow to build, split into chunks of 50
    - Process each chunk in separate requestIdleCallback frames
    - Show progress indicator during chunked build

[ ] Fix git push permission
    - Current origin: https://github.com/ohidgazi00003-gif/QuranMakerV3
    - Intended origin: https://github.com/mraselg/quran-editor-pro
    - Run: git remote set-url origin https://github.com/mraselg/quran-editor-pro

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
KEY FILES REFERENCE
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  src/lib/canvasMeasure.ts         ← OffscreenCanvas text measurement
  src/lib/textReflow.ts            ← uses canvas, not DOM span
  src/lib/quranLayout.ts           ← uses canvasMeasure import
  src/state/historyStore.ts        ← diff/patch based history (v3)
  src/state/reflowStore.ts         ← rebuildPage + idleCallback
  src/state/overridesStore.ts      ← exports: MASTER_DEFAULTS, setRestoringHistory, resetAll
  src/components/studio/PageList.tsx ← react-window v2 virtualized list (1740 pages)
  src/components/studio/CanvasToolbar.tsx ← History dropdown with Bengali labels
  src/tajweed/canvasMeasure.ts     ← Canvas char position for tajweed
  src/tajweed/measure.ts           ← Canvas primary, Range fallback
  src/data/dal.ts                  ← Data Access Layer interface

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
