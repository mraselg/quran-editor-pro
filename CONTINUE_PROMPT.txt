====================================================================
STUDIO AL-QALAM — HANDOFF DOCUMENT
Git Repo: https://github.com/mraselg/quran-editor-pro (branch: master)
Last verified: 2026-05-23 | TSC: clean (exit 0) | Build: clean (exit 0)
====================================================================

COMPLETED (verified in master):
  Phase 1 — commit 275511f
    - historyStore: session-only (no persist)
    - PropertiesPanel: async Reset All with loading states
    - localStorage old key cleanup on reset

  Phase 2 — commit 11473d4
    - overridesStore: arabicDx/Dy + banglaDx/Dy in LocalOverride
    - FabricLines: per-layer independent offsets applied in FabricRow
    - PropertiesPanel: Arabic X/Y and Bangla X/Y inputs in RowDetailSection

  TSC Fix — commit 8b037e7
    - dal.electron.ts: api() null-guard for window.electronAPI
    - tsc --noEmit now exits 0

NOT STARTED (for Lovable agent):
  Phase 3A — backFillFrom()
  Phase 3B — Smart Enter + LargeChangeGuard + ScopeImpactWarningDialog

====================================================================
CHECKER ROLE (Antigravity agent):
  - Run: npx tsc --noEmit (must stay exit 0)
  - Run: npm run build (must stay exit 0)
  - Fix TypeScript errors only
  - Create roadmap plans for Lovable agent — DO NOT implement features

IMPLEMENTER ROLE (Lovable agent):
  - Implement Phase 3A first, then 3B
  - Follow behavior contract below
  - git push after each phase

====================================================================
PHASE 3 BEHAVIOR CONTRACT:

  3A: backFillFrom()
      File: src/lib/textReflow.ts (add after reflowFrom)
      File: src/components/studio/FabricLines.tsx (checkOverflow integration)
      Rule: When delete leaves space > fontSize*0.8 → pull words from next row
      Rule: Respects maxPageId boundary (scope-limited)

  3B: Smart Enter
      scope=general  → push tail to row+1 only, no guard
      scope=page     → cascade to page end, guard if rows >= 20
      scope=surah    → ALWAYS show warning dialog first
      scope=global   → ALWAYS show warning dialog first

  Bengali strings (MUST NOT change):
      SCOPE_LABEL_BN = { general:"সাধারণ", page:"পেজ", surah:"সূরা", global:"সকল" }
      Dialog buttons: "না" | "হ্যাঁ, চালিয়ে যান"

  New files to create:
      src/hooks/useLargeChangeGuard.ts
      src/components/studio/ScopeImpactWarningDialog.tsx

====================================================================
PHASE 4 IDEAS (after Phase 3 done — pick one):
  A) Keyboard shortcuts: Alt+1/2/3/4 → scope switch
  B) Persist scope per-surah in localStorage
  C) Batch buildProgress updates (throttle with rAF for large globals)
  D) Scope-tagged Undo/Redo (historyStore already has scope field)
====================================================================
